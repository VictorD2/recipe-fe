import { FontType, SeparatorType } from "@/styles/types";

export const defaultFont: FontType = {
  color: "text-black",
  weight: "font-normal",
};

export const defaultSeparator: SeparatorType = {
  padding: "p-2",
};
